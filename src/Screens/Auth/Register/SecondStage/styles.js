import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  inputsWrapper: {
    marginVertical: 15,
    marginBottom: 45,
  },
  footerWrapper: {
    height: 50,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
